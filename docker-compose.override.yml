services:
  app:
    build:
      context: .
      target: dev
    command: ["sh", "-c", "npm run db:migrate && node --watch app.js --no-dotenv"]
    environment:
      NODE_ENV: "development"
    volumes:
      - ./:/app
      - /app/node_modules
